/** `"page"` → redirect to `/order/success`. `"modal"` → glass overlay on `/start` (Option B). */
export type OrderSuccessUxMode = "page" | "modal";

export const ORDER_SUCCESS_UX: OrderSuccessUxMode = "page";
